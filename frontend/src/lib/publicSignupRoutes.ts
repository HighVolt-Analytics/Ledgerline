/** Public self-serve signup (no invite token required). */
export const PUBLIC_SIGNUP_PATH = "/signup";

/** Tenant invite accept flow (requires ?token=). */
export const ACCEPT_INVITE_PATH = "/accept-invite";
