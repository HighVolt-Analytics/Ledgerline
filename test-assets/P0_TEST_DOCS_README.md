# P0 verification test documents

Generated by `generate_p0_verification_docs.py`. Nine PDFs (3 scenarios x PO/GRN/Invoice)
to exercise VR13 (bank-details fraud control) through the real ingest -> OCR -> match ->
validate pipeline, not just backend unit tests.

## 1. Set up two vendors in Vendor Master first

**Vendor A - "Ridgeline Test Supplies Pty Ltd"**
- ABN: `51 824 753 556`
- Bank: BSB `062-777`, Account `55667788`

**Vendor B - "Northgate Test Traders Pty Ltd"**
- ABN: `60 417 253 884`
- Bank: leave blank (do not enter any bank details)

## 2. Upload each scenario's PO -> GRN -> Invoice, in that order

| Scenario | Files | What it proves |
|---|---|---|
| Clean match | `p0test-po-A-CLEAN.pdf`, `p0test-grn-A-CLEAN.pdf`, `p0test-invoice-A-CLEAN.pdf` | Invoice bank (062-777/55667788) matches Vendor A's master exactly. VR13 should **pass**, 3-way match clean, invoice posts normally. |
| Bank mismatch (fraud) | `p0test-po-A-FRAUD.pdf`, `p0test-grn-A-FRAUD.pdf`, `p0test-invoice-A-FRAUD.pdf` | Same vendor, but invoice says "Remit to BSB 999-000 Account 90009000" - does not match master. VR13 should **hard-block** with "possible payment redirection fraud", and stay blocked even if you try to approve it from the Exceptions queue (unbypassable, same as VR12). |
| No bank on master | `p0test-po-B-NOBANK.pdf`, `p0test-grn-B-NOBANK.pdf`, `p0test-invoice-B-NOBANK.pdf` | Vendor B has no bank details in master at all, but the invoice has bank details (084-004/11220033). VR13 should **skip** (not block) - this is a data gap, not a detected mismatch. |

## 3. Where to look for the result

- Invoice detail page -> Validation panel -> VR13 row (pass / skipped / failed with message).
- For the fraud scenario: try approving from the Approvals/Exceptions queue - it should refuse, same as it would for a VR12 (unknown vendor) failure.
- Rule Book -> a document type -> Validation tab should now list "Bank details match" (VR13) as a configurable check.

## 4. After this, continue with the rest of the flow

Once the clean-match invoice (scenario A-CLEAN) posts, use it to continue the broader
checklist already sent (payment segregation of duties + RC1 reconciliation with
sub-ledger codes) - approve its payment as a *different* user than whoever approved
the invoice, and check the Reconciliation Overview for that date.
